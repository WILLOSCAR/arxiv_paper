# arXiv Paper Bot - 完整使用流程

## 📋 系统概述

这是一个**个性化 arXiv 论文推荐系统**，采用**漏斗筛选架构**，从每天500+篇论文中筛选出你最感兴趣的5-10篇。

### 核心特点
- ✅ **当前可用**：关键词过滤 + 用户反馈收集 + 多渠道推送
- 🔲 **预留槽位**：向量相似度排序 + Agent意图识别（待实现）

---

## 🚀 快速开始（3步上手）

### Step 1: 安装依赖
```bash
pip install -r requirements.txt
```

### Step 2: 配置关键词
编辑 `config/config.yaml`，设置你关心的：
- **categories**: 领域分类（如 cs.AI, cs.CV）
- **keywords**: 关键词及权重

### Step 3: 运行
```bash
python main.py
```

查看结果：
```bash
cat data/papers.json
```

---

## 📊 完整工作流程

### 阶段 0：配置阶段（首次使用）

```yaml
# config/config.yaml 核心配置

arxiv:
  categories:           # 你关注的领域
    - cs.CV            # 计算机视觉
    - cs.AI            # 人工智能
    - cs.LG            # 机器学习

  max_results: 50      # 每个类别最多抓取论文数

  fetch_mode: combined # 抓取模式（推荐combined）
    # category_only      : 只按分类抓取
    # keyword_only       : 只按关键词抓取
    # combined          : 分类+关键词（最高效 ⭐）
    # category_then_filter: 先抓全部再本地过滤

  fetch_full_categories: false # 关键词抓取时是否再拉分类全量兜底

  search_keywords:     # API级关键词（用于combined模式）
    - transformer
    - diffusion
    - multimodal

filter:
  enabled: true

  keywords:            # 本地过滤关键词（带权重）
    high_priority:     # 权重 3.0
      - transformer
      - diffusion
      - multimodal

    medium_priority:   # 权重 2.0
      - detection
      - segmentation

    low_priority:      # 权重 1.0
      - deep learning

  min_score: 1.0       # 最低分数阈值
  top_k: 20            # 保留前K篇

notification:          # 推送设置（可选）
  enabled: false
  provider: feishu     # feishu / telegram / wechat
  top_k: 5
  feishu:
    webhook_url: ""
    secret: ""
  telegram:
    bot_token: ""
    chat_id: ""
  wechat:
    app_id: ""
    app_secret: ""
    open_id: ""

personalization:       # 个性化设置（预留）
  enabled: false       # 🔲 暂未实现
  feedback:
    enabled: true      # ✅ 反馈收集已可用
```

---

### 阶段 1：每日抓取流程

```bash
# 默认抓取最近1天的论文
python main.py

# 抓取最近3天的论文
python main.py --days 3

# 测试模式（抓取少量论文验证配置）
python main.py --test
```

**执行过程：**
```
[1/5] 从 arXiv 抓取论文
  ├─ Mode: combined (cs.CV + keywords)
  ├─ Mode: combined (cs.AI + keywords)
  ├─ (可选) fetch_full_categories=True 时额外拉取分类全量数据
  └─ 去重 → 输出: ~50-100篇

[2/5] 本地关键词过滤与评分
  ├─ 标题+摘要关键词匹配
  ├─ 多级权重计算分数
  └─ Top-K 保留 → 输出: ~20篇

[3/5] AI总结生成（可选）
  └─ 默认关闭，需配置LLM API

[4/5] 保存结果
  ├─ data/papers.json (详细信息)
  └─ data/papers.csv  (表格格式)

[5/5] 多渠道推送（可选）
  └─ 根据 notification 配置自动推送 Top N 推荐
```

**输出示例：**
```
Top 5 Papers:

1. Artificial Hippocampus Networks for Efficient Long-Context...
   Score: 6.0
   Keywords: transformer, attention, neural network
   URL: https://arxiv.org/abs/2510.07318

2. Multimodal Diffusion Models for Image Generation
   Score: 5.0
   Keywords: diffusion, multimodal, generation
   URL: https://arxiv.org/abs/2510.xxxxx
...
```

---

### 阶段 2：查看和筛选论文

```bash
# 查看JSON格式（带评分）
cat data/papers.json | jq '.[] | {title, score, keywords: .matched_keywords}'

# 查看CSV格式（可用Excel打开）
open data/papers.csv

# 只看高分论文（分数>=5）
cat data/papers.json | jq '.[] | select(.score >= 5) | {title, score}'
```

---

### 阶段 3：用户反馈（关键！）

对看过的论文给出反馈，系统会学习你的偏好：

```bash
# 👍 喜欢这篇论文
python feedback.py like 2510.07318

# 👎 不喜欢这篇论文
python feedback.py dislike 2501.99999

# 📊 查看反馈统计
python feedback.py stats

# 输出：
# 📊 User Feedback Statistics
# ==================================================
#   Total liked:    15 papers
#   Total disliked: 3 papers
#   Like ratio:     83.3%
#
# 🔑 Top Keywords in Liked Papers:
#   - transformer          12 papers (80%)
#   - multimodal           8 papers (53%)
#   - vision-language      6 papers (40%)
```

```bash
# 查看已点赞的论文列表
python feedback.py list liked

# 查看不喜欢的论文列表
python feedback.py list disliked

# 查看所有反馈
python feedback.py list all

# 详细模式（显示关键词、时间）
python feedback.py list liked --verbose
```

```bash
# 清空点赞记录
python feedback.py clear liked

# 清空所有反馈（需确认）
python feedback.py clear all
```

**反馈数据存储位置：**
```
data/feedback/
├── liked_papers.json      # 点赞的论文
├── disliked_papers.json   # 不喜欢的论文
└── user_profile.json      # 用户画像（自动生成）
```

---

### 阶段 4：根据反馈调整（手动优化）

**当前阶段（Phase 0.5）- 手动调整：**

1. 查看你喜欢论文的高频关键词：
```bash
python feedback.py stats
```

2. 根据统计结果更新 `config.yaml`：
```yaml
filter:
  keywords:
    high_priority:
      - transformer    # 出现在80%的liked论文中 → 提升到high
      - multimodal     # 出现在53%的liked论文中 → 提升到high
```

3. 下次运行 `main.py` 时，系统会给这些关键词更高权重！

---

**未来阶段（Phase 1/2）- 自动调整：**

🔲 **Phase 1 - 向量相似度（预留）**：
```bash
# 1. 安装依赖
pip install sentence-transformers chromadb

# 2. 启用配置
# config.yaml: personalization.vector_ranking.enabled: true

# 3. 运行（自动根据liked论文重排序）
python main.py

# 原理：
# personalized_score = keyword_score × 0.6 + similarity_score × 0.4
```

🔲 **Phase 2 - Agent意图识别（预留）**：
```bash
# 1. 配置LLM
# config.yaml: personalization.agent.enabled: true

# 2. 运行（自动分析你的阅读模式）
python main.py --analyze-intent

# 输出：
# 📊 Reading Pattern Analysis:
#   Main interests: ["multimodal learning", "transformer"]
#   Suggested keywords: ["CLIP", "vision-language"]
#   → 自动更新搜索关键词
```

---

## 🎯 典型使用场景

### 场景 1：每天早上获取论文推荐

```bash
# 每天9点自动运行（Linux/Mac cron）
crontab -e

# 添加：
0 9 * * * cd /path/to/arxiv_paper && python main.py

# 或使用 schedule（参见 config.yaml schedule 配置）
```

### 场景 2：快速测试新关键词配置

```bash
# 1. 修改 config.yaml 添加新关键词
# 2. 测试模式抓取少量论文
python main.py --test

# 3. 检查结果是否符合预期
cat data/papers.json | jq '.[0:3]'

# 4. 满意后正式运行
python main.py
```

### 场景 3：周报整理（抓取一周论文）

```bash
# 抓取过去7天的论文
python main.py --days 7

# 导出为CSV供Excel分析
open data/papers.csv
```

### 场景 4：构建个人论文库

```bash
# 持续运行，数据累积到同一个文件
# config.yaml: storage.append_mode: true

# 运行1个月后，查看累积数据
cat data/papers.json | jq 'length'  # 例如：600篇

# 按日期分组统计
cat data/papers.json | jq 'group_by(.published[:10]) | .[] | {date: .[0].published[:10], count: length}'
```

---

## 📂 文件结构说明

```
arxiv_paper/
│
├── main.py                  # ✅ 主程序入口
├── feedback.py              # ✅ 反馈管理CLI
├── requirements.txt         # ✅ 依赖列表
│
├── config/
│   └── config.yaml          # ✅ 配置文件（核心）
│
├── src/                     # ✅ 核心模块
│   ├── fetcher.py           # 从arXiv抓取论文
│   ├── filter.py            # 关键词过滤与评分
│   ├── storage.py           # JSON/CSV存储
│   ├── models.py            # 数据模型
│   ├── feedback.py          # 反馈收集 ⭐
│   └── personalization.py   # 个性化（预留）🔲
│
├── data/                    # ✅ 数据目录（自动创建）
│   ├── papers.json          # 抓取的论文
│   ├── papers.csv
│   └── feedback/            # 反馈数据 ⭐
│       ├── liked_papers.json
│       ├── disliked_papers.json
│       └── user_profile.json
│
├── logs/                    # ✅ 日志目录
│   └── arxiv_bot.log
│
└── tests/                   # ✅ 单元测试
    └── test_*.py            # 19个测试全部通过
```

---

## ⚙️ 配置说明：4种抓取模式对比

| 模式 | API查询 | 本地过滤 | 效率 | 适用场景 |
|------|---------|----------|------|----------|
| **category_only** | `cat:cs.AI` | ✅ | 中 | 只关心领域，不限关键词 |
| **keyword_only** | `"transformer" OR "diffusion"` | ✅ | 中 | 跨领域关键词搜索 |
| **combined** ⭐ | `("transformer") AND cat:cs.AI` | ✅ | **高** | 既限领域又限关键词（推荐） |
| **category_then_filter** | `cat:cs.AI` | ✅ | 低 | 兼容模式（不推荐） |

**推荐使用 `combined` 模式**：
- API级别就过滤掉90%无关论文
- 减少网络传输和本地计算
- 最高效！

---

## 🔄 数据流图解

```
┌─────────────────────────────────────────────────┐
│  500+ Daily Papers on arXiv                     │
└─────────────────────────────────────────────────┘
                    ↓
         ┌──────────────────────┐
         │  Fetch Mode Selection │
         └──────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  【漏斗 Level 1】API-Level Filtering             │
│  ─────────────────────────────────────────      │
│  • combined: (keywords) AND cat:cs.AI           │
│  • Efficiency: Reduce 90% papers                │
│  ─────────────────────────────────────────      │
│  Output: ~50-100 papers                         │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  【漏斗 Level 2】Local Keyword Scoring           │
│  ─────────────────────────────────────────      │
│  • Match keywords in title + abstract           │
│  • Score = Σ(weight × match)                    │
│  • Top-K selection                              │
│  ─────────────────────────────────────────      │
│  Output: ~20 papers                             │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  【槽位 1】Vector Similarity Re-ranking  🔲      │
│  ─────────────────────────────────────────      │
│  • Compute embeddings (SPECTER)                 │
│  • Similarity to liked papers                   │
│  • personalized_score = kw×0.6 + sim×0.4        │
│  ─────────────────────────────────────────      │
│  Status: Reserved (Phase 1)                     │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  【槽位 2】Agent Intent Recognition  🔲          │
│  ─────────────────────────────────────────      │
│  • LLM analyzes reading patterns                │
│  • Generate dynamic keywords                    │
│  • Provide recommendation reasons               │
│  ─────────────────────────────────────────      │
│  Status: Reserved (Phase 2)                     │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  📋 Final Personalized List (5-10 papers)       │
│  ─────────────────────────────────────────      │
│  • Save to data/papers.json                     │
│  • Display top 5 in console                     │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│  👍👎 User Feedback Loop                        │
│  ─────────────────────────────────────────      │
│  • python feedback.py like <paper_id>           │
│  • Build user profile                           │
│  • Analyze keyword preferences                  │
│  ─────────────────────────────────────────      │
│  → Feeds into Phase 1/2 when implemented        │
└─────────────────────────────────────────────────┘
```

---

## 🎯 核心流程总结

### ✅ 当前可用流程（Phase 0.5）

```bash
# 1. 配置关键词
vim config/config.yaml

# 2. 抓取论文
python main.py

# 3. 查看结果
cat data/papers.json | jq '.[] | {title, score}'

# 4. 反馈收集
python feedback.py like 2510.07318
python feedback.py stats

# 5. 手动优化关键词（根据stats结果）
vim config/config.yaml

# 6. 循环重复 2-5
```

### 🔲 未来流程（Phase 1 自动化）

```bash
# 1. 启用向量相似度
# config.yaml: personalization.vector_ranking.enabled: true

# 2. 运行（自动根据liked论文调整）
python main.py

# 3. 系统自动：
#    - 计算论文embedding
#    - 与liked论文比较相似度
#    - 自动重排序
```

---

## ❓ 常见问题

**Q1: 为什么我fetch到0篇论文？**
```bash
# 可能原因1：days太小，最近没有新论文
python main.py --days 7  # 改用7天

# 可能原因2：关键词太严格
# 修改 config.yaml，减少或放宽关键词

# 可能原因3：网络问题
# 检查能否访问 export.arxiv.org
```

**Q2: 如何调整论文数量？**
```yaml
# config.yaml
arxiv:
  max_results: 100  # 增加API抓取数量

filter:
  top_k: 50         # 增加最终保留数量
  min_score: 0.5    # 降低分数阈值
```

**Q3: personalization功能什么时候可用？**
```
✅ Phase 0.5 (当前): 关键词过滤 + 反馈收集
🔲 Phase 1 (待实现): 向量相似度排序（需1-2天开发）
🔲 Phase 2 (待实现): Agent意图识别（需3-5天开发）

预留接口已完成，实现时只需：
1. 取消注释 requirements.txt 中的依赖
2. 实现 src/personalization.py 中的TODO方法
```

**Q4: 如何备份我的反馈数据？**
```bash
# 打包反馈数据
tar -czf feedback_backup.tar.gz data/feedback/

# 恢复
tar -xzf feedback_backup.tar.gz
```

**Q5: 论文数据会去重吗？**
```
✅ 会！
- 同一次运行内，按 arxiv_id 去重
- append_mode: true 时，存储模块也会去重
```

---

## 📚 相关文档

- **ARCHITECTURE.md** - 详细架构设计与预留槽位说明
- **IMPLEMENTATION_SUMMARY.md** - 完整实现总结与Phase规划
- **RESEARCH.md** - arXiv API调研与工具对比
- **README.md** - 项目简介与特性说明

---

## 🎉 总结

**当前系统已完成：**
1. ✅ 高效的多模式论文抓取（combined模式推荐）
2. ✅ 灵活的关键词过滤与多级评分
3. ✅ 完整的用户反馈收集系统
4. ✅ JSON/CSV双格式存储
5. ✅ 清晰的预留槽位架构

**下一步：**
- 持续使用反馈系统，积累偏好数据
- 根据统计结果手动优化关键词配置
- 待积累足够数据后，实现Phase 1向量相似度功能

**使用建议：**
- 每天运行 `python main.py`
- 认真标记 `like/dislike`（这是个性化的基础！）
- 每周查看 `python feedback.py stats` 调整配置

---

**版本**: v0.1.0 (Phase 0.5)
**状态**: ✅ 生产可用 | 🔲 预留槽位已就绪
**最后更新**: 2025-10-09
