# Archive

This folder stores legacy docs and utilities retained for reference.

- `legacy_docs/`: superseded design/operation notes
- `legacy_tools/`: old experiment/debug scripts

Policy:
- Archived files are not part of the primary supported workflow.
- Secrets should never be hardcoded, even in archived files.
- After one or more stable iterations, archived files can be deleted.
